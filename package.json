{
  "name": "fxdevkit-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "plugins/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build -w @fxdevkit/sdk && npm run build -w @fxdevkit/core && npm run build -w @fxdevkit/cli && npm run build -w @fxdevkit/plugin-commit-rules",
    "clean": "rimraf packages/*/dist 2>/dev/null || true",
    "test": "node --test \"packages/core/test/*.test.js\" \"plugins/commit-rules/test/*.test.js\""
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "yaml": "^2.5.0"
  }
}
